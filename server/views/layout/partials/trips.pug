.row.proximos-viajes
    each trip in trips
        .col-md-6.col-lg-4.mb-4
            .card
                img(src=`img/destinos_${trip.image}.jpg`, class="img-fluid")
                .card-body
                    h2=trip.title
                    p 
                        i.fas.fa-calendar-alt 
                        |   #{trip.departure_date} -  #{trip.date_of_return}
                    p 
                        i.fas.fa-dollar-sign 
                        |   #{trip.price}
                    p= trip.description.substr(0,100)
                    a(href=`viajes/${trip.id}`, class="btn btn-success btn-block") Más información